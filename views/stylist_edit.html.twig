<!DOCTYPE html>
<html>
    <head>
      <link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css">
        <meta charset="utf-8">
        <title>Edit Stylist</title>
    </head>
    <body>
        <h1>Change {{ stylist.getName }} the stylist's name!</h1>

        <form action="/stylists/{{ stylist.getId }}" method="post">
            <input name="_method" type="hidden" value="patch">

            <label for="name">Rename {{ stylist.getName }}</label>
            <input id="name" name="name" type="text">

            <button type="submit">Give {{ stylist.getName }} that new name for real</button>
        </form>

        <form action="/stylists/{{ stylist.getId }}" method="post">
            <input name="_method" type="hidden" value="delete">

            <button type="submit">Delete {{ stylist.getName }}</button>
        </form>

    </body>
</html>
